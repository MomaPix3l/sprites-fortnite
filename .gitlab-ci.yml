pages:
  stage: deploy
  script:
    - echo "Deploying static Fortnite Sprite Tracker to GitLab Pages"
  artifacts:
    paths:
      - public
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
